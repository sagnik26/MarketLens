/** Zod validation middleware: validateBody and validateQuery for request payloads. */

export async function validateBody(): Promise<unknown> {
  return {};
}

export function validateQuery(): unknown {
  return {};
}
