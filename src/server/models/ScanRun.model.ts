/** Mongoose ScanRun schema: status, triggeredBy, pageResults (JSON), externalRunId, timestamps. */

export const ScanRunModel = {} as unknown as { create: () => unknown; findByIdAndUpdate: () => unknown };
