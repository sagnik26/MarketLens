/** Mongoose User schema and model; timestamps, toJSON transform (id, no password), indexes. */

export const UserModel = {} as unknown as { findById: () => unknown; findOne: () => unknown; create: () => unknown };
