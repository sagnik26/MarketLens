/** All DB queries for Competitor and CompetitorPage; always filter by companyId first. */

export const competitorRepository = {
  findMany: async () => ([]),
  findById: async () => null,
  findPagesByCompetitorId: async () => ([]),
};
