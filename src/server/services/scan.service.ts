/** Orchestrates TinyFish via client: creates ScanRun, runs agents (run-sse/run-async), updates status. */

export const scanService = {
  runCompetitorScan: async () => ({ scanRunId: "", status: "" }),
};
